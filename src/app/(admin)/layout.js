export const metadata = {
    title: "SubPanel Admin",
    description: "Subpanel Admin",
};

export default function AdminLayout({ children }) {
    return (
        <>
            {children}
        </>
    );
}